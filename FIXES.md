# 🔧 Исправления проблем

## ✅ Исправленные проблемы

### 1. Ошибка checksum адреса Factory для BSC
**Проблема:** `bad address checksum (argument="address", value="0xcA143Ce0Fe65960E6Aa4D42C8D3cE161c2B6604f")`

**Исправление:** 
- Изменен адрес Factory с `0xcA143Ce0Fe65960E6Aa4D42C8D3cE161c2B6604f` 
- На правильный адрес с checksum: `0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73`

**Файл:** `bot/config.js` (строка 22)

### 2. Неправильный gasPrice
**Проблема:** В логе показывалось `gasPrice: 5000000000000000000n` (5000 gwei вместо 5 gwei)

**Исправление:**
- Изменен gasPrice в config с `'5000000000'` на `'5'` (в gwei)
- Теперь правильно парсится через `ethers.parseUnits('5', 'gwei')` = 5000000000 wei = 5 gwei

**Файл:** `bot/config.js` (строка 25)

**Пояснение:**
- `5 gwei` = `5000000000 wei` = `5 * 10^9 wei`
- В конфиге теперь указывается `'5'` (gwei), который преобразуется в wei через `ethers.parseUnits()`

### 3. DeprecationWarning для answerCallbackQuery
**Проблема:** `The method signature answerCallbackQuery(callbackQueryId, text, showAlert) has been deprecated since v0.27.1`

**Исправление:**
- Обновлены все вызовы `answerCallbackQuery(callbackQueryId, text)` 
- На новый формат: `answerCallbackQuery(callbackQueryId, { text: '...' })`

**Файл:** `bot/telegram-bot.js` (12 мест)

### 4. Ошибка EPERM при сохранении users.json
**Проблема:** `EPERM: operation not permitted, open 'D:\multizap-bot\bot\users.json'`

**Исправление:**
- Добавлена проверка существования директории
- Добавлено создание файла, если его нет
- Добавлена обработка ошибок с попыткой создания файла
- Установлены права доступа `0o666` для файла

**Файл:** `bot/userManager.js` (метод `saveUsers()`)

## 📊 Проверка исправлений

### Gas Price
```javascript
// В config.js указано: '5' (gwei)
// После parseUnits('5', 'gwei') получаем: 5000000000 wei
// Это правильно: 5 gwei = 5 * 10^9 wei
```

### Factory Address
```javascript
// Старый (неправильный): 0xcA143Ce0Fe65960E6Aa4D42C8D3cE161c2B6604f
// Новый (правильный):    0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73
```

## 🧪 Тестирование

После исправлений попробуйте снова:

1. **Развертывание контракта в BSC:**
   ```bash
   # В Telegram боте:
   /network  # Выберите BSC
   /deploy   # Должно работать без ошибки checksum
   ```

2. **Проверка gasPrice:**
   - В логах должно быть: `gasPrice: 5000000000n` (5 gwei в wei)
   - Не должно быть: `5000000000000000000n` (5000 gwei)

3. **Проверка users.json:**
   - Файл должен создаваться автоматически
   - Не должно быть ошибок EPERM

4. **Проверка answerCallbackQuery:**
   - Не должно быть DeprecationWarning в консоли

## ⚠️ Важные замечания

1. **Gas Price для BSC:**
   - По умолчанию: 5 gwei
   - Можно изменить через переменную окружения: `BSC_GAS_PRICE=3` (в gwei)
   - Для других сетей (ETH, BASE) gasPrice = null (автоматический расчет)

2. **Factory Address:**
   - Убедитесь, что используете правильный адрес с checksum
   - Для BASE адреса могут потребовать проверки

3. **Права доступа:**
   - Если все еще есть проблемы с users.json, проверьте права на папку `bot/`
   - На Windows может потребоваться запуск от имени администратора

## 🔄 Что делать дальше

1. Перезапустите бота
2. Попробуйте развернуть контракт в BSC
3. Проверьте логи - не должно быть ошибок checksum и gasPrice должен быть правильным
4. Проверьте, что users.json создается и сохраняется

