# 🧪 Инструкция по тестированию мультичейн бота

## ✅ Автоматические тесты

Запустите автоматические тесты:
```bash
npm run test-multichain
```

Это проверит:
- ✅ Конфигурацию всех сетей (ETH, BSC, BASE)
- ✅ Переключение сетей в Web3Manager
- ✅ Работу UserManager с несколькими сетями
- ✅ Подключение к RPC всех сетей

## 📱 Тестирование в Telegram боте

### 1. Запуск бота

```bash
npm run bot
```

Или:
```bash
node bot/telegram-bot.js
```

### 2. Базовое тестирование

#### Шаг 1: Проверка команд
1. Откройте Telegram и найдите вашего бота
2. Отправьте `/start` - должно показать приветственное сообщение
3. Отправьте `/home` - должно показать главное меню с текущей сетью

#### Шаг 2: Переключение сетей
1. Отправьте `/network` или нажмите кнопку "🌐 Сеть" в главном меню
2. Выберите сеть (ETH, BSC или BASE)
3. Проверьте, что сеть изменилась
4. Отправьте `/home` снова - должна отображаться новая сеть

#### Шаг 3: Регистрация
1. Отправьте `/register` или нажмите "🔐 Регистрация"
2. Введите ваш приватный ключ (тестовый или реальный)
3. Должно появиться сообщение об успешной регистрации

#### Шаг 4: Развертывание контракта
1. Выберите сеть через `/network`
2. Отправьте `/deploy` или нажмите "🚀 Развернуть контракт"
3. Дождитесь развертывания (может занять время)
4. Проверьте, что адрес контракта отображается с правильным explorer URL:
   - ETH → etherscan.io
   - BSC → bscscan.com
   - BASE → basescan.org

#### Шаг 5: Тестирование в разных сетях
1. Разверните контракт в BSC
2. Переключитесь на BASE через `/network`
3. Попробуйте развернуть контракт в BASE (должен быть отдельный контракт)
4. Переключитесь обратно на BSC
5. Проверьте, что контракт BSC все еще доступен

### 3. Тестирование операций

#### Добавление токена
1. Выберите сеть
2. Убедитесь, что контракт развернут в этой сети
3. Отправьте `/addtoken`
4. Введите адрес токена
5. Проверьте, что ссылка на транзакцию ведет на правильный explorer

#### Zap-in операция
1. Отправьте `/zapin` или выберите токен через `/positions`
2. Введите адрес токена и сумму
3. Проверьте, что:
   - Сумма отображается в правильной валюте (ETH/BNB)
   - Ссылка на транзакцию ведет на правильный explorer

#### Exit операция
1. Отправьте `/exit` или выберите токен через `/positions`
2. Введите адрес токена
3. Проверьте ссылку на транзакцию

#### Балансы
1. Отправьте `/balance`
2. Проверьте, что:
   - Отображается правильная сеть
   - Валюта указана правильно (ETH/BNB)

### 4. Проверка edge cases

#### Тест 1: Переключение без контракта
1. Переключитесь на сеть, где нет контракта
2. Попробуйте выполнить операцию (должна быть ошибка)
3. Разверните контракт
4. Попробуйте снова (должно работать)

#### Тест 2: Контракты в разных сетях
1. Разверните контракт в BSC
2. Переключитесь на BASE
3. Разверните контракт в BASE
4. Переключайтесь между сетями - каждый контракт должен сохраняться

#### Тест 3: Неправильная сеть
1. Попробуйте использовать токен из одной сети в другой
2. Должна быть ошибка (токены не пересекаются между сетями)

## 🔍 Что проверить

### В сообщениях бота:
- ✅ Текущая сеть отображается в главном меню
- ✅ Правильная валюта (ETH/BNB) в сообщениях
- ✅ Правильные ссылки на explorer (etherscan/bscscan/basescan)
- ✅ Контракты сохраняются для каждой сети отдельно

### В логах консоли:
- ✅ Нет ошибок при переключении сетей
- ✅ Правильные RPC URL используются
- ✅ Правильные газовые параметры для каждой сети

## ⚠️ Важные замечания

1. **Для каждой сети нужен отдельный контракт** - разверните контракт в каждой сети отдельно
2. **Токены не пересекаются** - токены из одной сети не работают в другой
3. **Газ в разных сетях** - убедитесь, что у вас есть нативная валюта для газа в каждой сети
4. **BASE может требовать настройки** - адреса Router/Factory для BASE могут потребовать уточнения

## 🐛 Если что-то не работает

1. Проверьте логи консоли на ошибки
2. Убедитесь, что RPC URL доступны
3. Проверьте, что контракт развернут в нужной сети
4. Проверьте баланс нативной валюты для газа
5. Используйте `/network` для проверки текущей сети

## 📝 Чеклист тестирования

- [ ] Автоматические тесты прошли успешно
- [ ] Бот запускается без ошибок
- [ ] Команда `/network` работает
- [ ] Переключение сетей работает
- [ ] Главное меню показывает текущую сеть
- [ ] Регистрация работает
- [ ] Развертывание контракта работает в BSC
- [ ] Развертывание контракта работает в BASE
- [ ] Развертывание контракта работает в ETH
- [ ] Ссылки на explorer правильные для каждой сети
- [ ] Валюта отображается правильно (ETH/BNB)
- [ ] Контракты сохраняются отдельно для каждой сети
- [ ] Операции работают в каждой сети

