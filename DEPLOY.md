# 🚀 Инструкция по деплою на сервер

## Шаг 1: Подготовка изменений

Все изменения готовы к коммиту. Выполните следующие команды:

```bash
# Добавить все изменения
git add .

# Создать коммит с описанием изменений
git commit -m "feat: Добавлена поддержка мультичейн (ETH, BSC, BASE)

- Добавлена поддержка сетей ETH, BSC, BASE
- Реализовано переключение сетей в Telegram боте
- Улучшена обработка ошибок (rate limit, MESSAGE_TOO_LONG)
- Обновлены кнопки покупки (0.01, 0.05, 0.02, 0.04)
- Автоматическое открытие позиции после покупки
- Добавлена диагностика проблем с Factory при продаже
- Улучшены сообщения об ошибках"

# Отправить изменения на сервер
git push origin main
```

## Шаг 2: Настройка переменных окружения на Render.com

После пуша, Render.com автоматически начнет деплой (если `autoDeploy: true`).

**ВАЖНО:** Нужно добавить новые переменные окружения в настройках сервиса на Render.com:

### Для мультичейн поддержки добавьте:

#### Ethereum (ETH):
- `ETH_RPC_URL` - RPC URL для Ethereum (например: `https://eth.llamarpc.com`)
- `ETH_ROUTER_ADDRESS` - Адрес Uniswap V2 Router (опционально, по умолчанию используется из конфига)
- `ETH_FACTORY_ADDRESS` - Адрес Uniswap V2 Factory (опционально)

#### Binance Smart Chain (BSC):
- `BSC_RPC_URL` - RPC URL для BSC (например: `https://bsc-dataseed1.binance.org`)
- `BSC_ROUTER_ADDRESS` - Адрес PancakeSwap V2 Router (опционально)
- `BSC_FACTORY_ADDRESS` - Адрес PancakeSwap V2 Factory (опционально)

#### Base:
- `BASE_RPC_URL` - RPC URL для Base (например: `https://mainnet.base.org`)
- `BASE_ROUTER_ADDRESS` - Адрес Uniswap V2 Router на Base (опционально)
- `BASE_FACTORY_ADDRESS` - Адрес Uniswap V2 Factory на Base (опционально)

### Старые переменные (для обратной совместимости):
- `RPC_URL` - используется как fallback для BSC
- `CHAIN_ID` - используется как fallback
- `ROUTER_ADDRESS` - используется как fallback для BSC
- `FACTORY_ADDRESS` - используется как fallback для BSC

### Обязательные переменные:
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `PRIVATE_KEY` - приватный ключ для подписи транзакций (если используется)

## Шаг 3: Проверка деплоя

1. Откройте панель Render.com
2. Перейдите в ваш сервис `multizap-bot`
3. Проверьте логи деплоя
4. Убедитесь, что все переменные окружения установлены
5. Проверьте, что бот запустился без ошибок

## Шаг 4: Тестирование

После успешного деплоя протестируйте:

1. Откройте Telegram бота
2. Отправьте `/start`
3. Проверьте команду `/network` - должно быть переключение сетей
4. Проверьте `/home` - должна отображаться текущая сеть
5. Попробуйте развернуть контракт в разных сетях
6. Проверьте покупку и продажу токенов

## Альтернативный способ (SSH на сервер)

Если вы деплоите на свой сервер через SSH:

```bash
# 1. На вашем компьютере
git push origin main

# 2. Подключитесь к серверу
ssh user@your-server.com

# 3. Перейдите в директорию проекта
cd /path/to/multizap-bot

# 4. Получите последние изменения
git pull origin main

# 5. Установите зависимости (если нужно)
npm install

# 6. Перезапустите бота
pm2 restart multizap-bot
# или
systemctl restart multizap-bot
# или просто
node bot/telegram-bot.js
```

## Откат изменений (если что-то пошло не так)

Если нужно откатить изменения:

```bash
# Посмотреть историю коммитов
git log --oneline

# Откатиться на предыдущий коммит
git reset --hard HEAD~1

# Или откатиться на конкретный коммит
git reset --hard <commit-hash>

# Принудительно отправить на сервер (ОСТОРОЖНО!)
git push origin main --force
```

## Полезные команды Git

```bash
# Посмотреть статус
git status

# Посмотреть изменения в файле
git diff bot/telegram-bot.js

# Посмотреть историю
git log --oneline -10

# Создать новую ветку для тестирования
git checkout -b test-multichain
git push origin test-multichain
```

## Примечания

- **Не коммитьте `.env` файл** - он уже в `.gitignore`
- **Не коммитьте `bot/users.json`** - он содержит приватные ключи пользователей
- **Проверьте `render.yaml`** - там должны быть все нужные переменные окружения
- **После деплоя проверьте логи** - там могут быть ошибки, которые нужно исправить

